---
name: "auto-dev-server"
description: "Automatically starts frontend and backend dev servers together. Invoke when developing full-stack apps or needing both services running."
---

# 自动化开发服务器启动

## 功能介绍

这个技能帮助你同时启动前端和后端开发服务器，简化全栈应用的开发流程。

## 使用场景

- 开发全栈应用时，需要同时启动前后端服务
- 不想手动打开多个终端分别启动服务
- 需要前后端服务联动控制（同时启动、同时停止）

## 快速开始

### 1. 创建配置文件

在项目根目录创建 `auto-dev-server.config.json`：

```json
{
  "frontend": {
    "command": "npm run dev",
    "cwd": ".",
    "readyPattern": "Local:   http://localhost:"
  },
  "backend": {
    "command": "npm run dev",
    "cwd": "./server",
    "readyPattern": "服务器启动成功"
  }
}
```

### 2. 使用技能启动服务

```bash
# 使用配置文件启动
auto-dev-server

# 或使用简写命令
dev
```

## 配置选项

### 完整配置示例

```json
{
  "frontend": {
    "command": "npm run dev",
    "cwd": ".",
    "readyPattern": "ready in",
    "env": {
      "PORT": "5173",
      "VITE_API_URL": "http://localhost:5000"
    }
  },
  "backend": {
    "command": "node simple-server.js",
    "cwd": "./server",
    "readyPattern": "服务器启动成功",
    "env": {
      "PORT": "5000",
      "NODE_ENV": "development"
    }
  },
  "timeout": 30000
}
```

### 配置说明

| 选项 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `frontend.command` | string | 是 | - | 前端启动命令 |
| `frontend.cwd` | string | 否 | `.` | 前端工作目录 |
| `frontend.readyPattern` | string | 否 | `ready in` | 检测前端就绪的模式 |
| `frontend.env` | object | 否 | `{}` | 前端环境变量 |
| `backend.command` | string | 是 | - | 后端启动命令 |
| `backend.cwd` | string | 否 | `./server` | 后端工作目录 |
| `backend.readyPattern` | string | 否 | `服务器启动成功` | 检测后端就绪的模式 |
| `backend.env` | object | 否 | `{}` | 后端环境变量 |
| `timeout` | number | 否 | `30000` | 启动超时时间（毫秒） |

## 常用命令

```bash
# 使用默认配置启动
auto-dev-server

# 指定配置文件
auto-dev-server --config my-config.json

# 查看帮助
auto-dev-server --help

# 查看版本
auto-dev-server --version
```

## 快捷键

- `Ctrl + C` - 停止所有服务并退出
- `r` - 重启后端服务
- `R` - 重启所有服务
- `l` - 清除日志屏幕

## 输出示例

```
╔═══════════════════════════════════════════════════════════╗
║           🚀 自动化开发服务器启动中...                      ║
╚═══════════════════════════════════════════════════════════╝

[12:34:56] [前端] 🟢 正在启动...
[12:34:57] [前端] Vite v5.4.11  ready in 1192 ms
[12:34:57] [前端] ✅ 前端服务就绪 - http://localhost:8080
[12:34:57] [后端] 🟢 正在启动...
[12:34:58] [后端] 服务器启动成功，端口: 5000
[12:34:58] [后端] ✅ 后端服务就绪 - http://localhost:5000

╔═══════════════════════════════════════════════════════════╗
║              ✅ 所有服务已就绪！                             ║
║                                                              ║
║  前端: http://localhost:8080                                ║
║  后端: http://localhost:5000                                ║
║  按 Ctrl+C 停止所有服务                                      ║
╚═══════════════════════════════════════════════════════════╝
```

## 故障排查

### 问题：端口被占用

**症状**: `Error: listen EADDRINUSE: address already in use :::5000`

**解决方法**:
```bash
# Windows
netstat -ano | findstr :5000
taskkill /PID <进程ID> /F

# Mac/Linux
lsof -ti :5000 | xargs kill -9
```

### 问题：后端启动失败

**症状**: 后端服务无法启动或立即退出

**检查清单**:
1. 确认后端依赖已安装：`cd server && npm install`
2. 检查后端配置文件是否正确
3. 查看后端日志中的错误信息
4. 确认端口没有被占用

### 问题：前端就绪检测超时

**症状**: `前端启动超时，请检查服务是否正常`

**解决方法**:
1. 调整 `readyPattern` 匹配前端实际输出
2. 增加 `timeout` 配置值
3. 手动检查前端是否能正常启动

## 项目规则

使用本技能时，请确保遵循项目开发规范：
- 前端代码放在 `src/` 目录
- 后端代码放在 `server/` 目录
- 配置文件放在项目根目录
- 使用 Git 进行版本控制
